export interface Pagination {
    page: number,
    itemsPerPage: number,
    totalElements: number,
    totalPages: number, 
    sortBy: string         
}                                                     