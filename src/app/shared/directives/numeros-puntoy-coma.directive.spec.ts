import { NumerosPuntoyComaDirective } from './numeros-puntoy-coma.directive';

describe('NumerosPuntoyComaDirective', () => {
  it('should create an instance', () => {
    const directive = new NumerosPuntoyComaDirective();
    expect(directive).toBeTruthy();
  });
});
