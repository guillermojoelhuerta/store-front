import { MascaraMonedaDirective } from './mascara-moneda.directive';

describe('MascaraMonedaDirective', () => {
  it('should create an instance', () => {
    const directive = new MascaraMonedaDirective();
    expect(directive).toBeTruthy();
  });
});
