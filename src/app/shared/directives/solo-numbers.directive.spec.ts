import { SoloNumbersDirective } from './solo-numbers.directive';

describe('SoloNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new SoloNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
